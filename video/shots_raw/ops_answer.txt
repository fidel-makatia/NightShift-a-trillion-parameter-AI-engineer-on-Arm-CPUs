`find /var/log -type f -exec ls -lh {} + | awk '{print $5, $6, $7, $8, $9}' | sort -hr | head -10`
This doesn't handle ties by newest first though. Let me think again.

We need to sort by size primarily, but for files of the same size, sort by modification time (newest first). The `sort` command can
